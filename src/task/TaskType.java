package task;

/**
 * The type of the task:
 * <br>{@code TASK} — Simple task
 * <br>{@code EPIC} — Epic meta-task
 * <br>{@code SUBTASK} — Subtask of the Epic
 */
public enum TaskType {
    TASK,
    EPIC,
    SUBTASK
}
